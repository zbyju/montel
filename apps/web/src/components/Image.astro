---
interface Props {
  src: string;
  alt: string;
  className?: string;
}
const { src, alt, className } = Astro.props;
---

<img
  src={src}
  alt={alt}
  class={`${className} rounded-lg shadow-md hover:shadow-lg transition-all duration-500`}
/>
