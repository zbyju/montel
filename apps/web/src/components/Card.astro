---
import Link from "./Link.astro";

interface Props {
  header: string;
  description: string;
  bg: string;
}

const { header, description, bg } = Astro.props;
---

<div
  class="w-64 h-100 bg-white shadow-md rounded-lg hover:shadow-lg transition-all duration-300"
>
  <div class="h-full p-5 flex flex-col justify-between">
    <div>
      <div class={`w-10 h-[50px] text-rose-600 mb-3`}>
        <slot name="icon" />
      </div>
      <h3 class="text-blue-900 font-bold text-2xl mb-1">{header}</h3>
      <p class="mb-2 text-blue-900">{description}</p>
    </div>
    <Link href="/babybox" bg={bg} className="self-start">Více</Link>
  </div>
</div>
