---
import Link from "./Link.astro";

interface Props {
  header: string;
  description: string;
}

const { header, description } = Astro.props;
---

<div
  class="w-64 h-100 bg-cyan-50 shadow-md hover:shadow-lg transition-all duration-300"
>
  <div class="h-full p-5 flex flex-col justify-between">
    <div class="w-12 text-rose-600 mb-8">
      <slot name="icon" />
    </div>
    <h3 class="text-blue-900 font-bold text-2xl">{header}</h3>
    <p class="mb-2 text-cyan-800">{description}</p>
    <Link href="/babybox">Více</Link>
  </div>
</div>
